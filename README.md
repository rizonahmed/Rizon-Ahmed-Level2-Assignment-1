# 🎯 TypeScript Conceptual Blog (Assignment Task)

এই লেখায় আমি TypeScript-এর দুটি গুরুত্বপূর্ণ টপিক সহজভাবে ব্যাখ্যা করেছি:  
**(১) Interface এবং Type এর পার্থক্য**  
**(২) keyof কীওয়ার্ডের ব্যবহার এবং উদাহরণ**

---

## 🔶 ১. TypeScript-এ Interface এবং Type এর মধ্যে মূল পার্থক্য

TypeScript-এ `interface` এবং `type alias` উভয়ই কোনো ডেটার কাঠামো নির্ধারণ করতে ব্যবহৃত হয়। তবে তাদের ক্ষমতা ও ব্যবহারের ক্ষেত্রে কিছু উল্লেখযোগ্য পার্থক্য রয়েছে।

### ✔ Declaration Merging
- Interface একাধিকবার ঘোষণা করা হলে স্বয়ংক্রিয়ভাবে merge হয়ে যায়।
- Type alias merge হয় না; একই নামে পুনরায় লিখলে error দেয়।

### ✔ Extending / Inheritance
- Interface খুব স্বাভাবিকভাবেই `extends` ব্যবহার করে আরেকটি interface থেকে structure নিতে পারে।
- Type alias ও extend করা যায়, তবে তার জন্য intersection (`&`) ব্যবহার করতে হয়।

### ✔ ব্যবহারের ক্ষেত্র
- যখন কেবল object-এর structure নির্ধারণ করতে হয়, তখন interface ব্যবহার করা উত্তম।
- যখন union, primitive, tuple অথবা complex টাইপ নিয়ে কাজ করতে হয়, তখন type alias আরও বেশি নমনীয়।

সংক্ষেপে, interface object pattern-এর জন্য বেশি উপযোগী, আর type alias multi-purpose টাইপ ব্যবহারের জন্য উপযোগী।

---

## 🔶 ২. keyof কীওয়ার্ডের ব্যবহার

`keyof` হলো TypeScript-এর একটি utility operator, যা কোনো type-এর সমস্ত key-কে একটি ইউনিয়ন টাইপ হিসেবে রিটার্ন করে।  
এটি type-safe attribute access নিশ্চিত করতে খুব কার্যকর।

### ✔ Example

```ts
type User = {
  name: string;
  age: number;
  email: string;
};

type UserKeys = keyof User;
// "name" | "age" | "email"

function getValue(obj: User, key: UserKeys) {
  return obj[key];
}
